# PORT: Optional local server port.
# Default: 3000
PORT=3000

# Optional. Keep false/off for first launch privacy posture.
# When true, server stores submitted work in .data/submissions.json.
ENABLE_SERVER_LEDGER=false
